import React from 'react';
import '../styles/TopBar.css';

const TopBar = () => {

    return (
        <div className="top-bar">
            <div className="top-bar-content">
                <h2 className="top-bar-title">WEATHER TUNES</h2>
            </div>
        </div>
    );
};

export default TopBar;
